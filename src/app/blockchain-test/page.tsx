import { BlockchainBatchUploader } from '@/components/farmer/blockchain-batch-uploader';

export default function BlockchainTestPage() {
  return (
    <main className="min-h-screen bg-gray-50 py-8">
      <BlockchainBatchUploader />
    </main>
  );
}
